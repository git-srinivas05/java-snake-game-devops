pipeline {
    agent any

    stages {
        stage('Build Docker Image') {
            steps {
                script {
                    docker.build("snake-game-java")
                }
            }
        }

        stage('Run Container') {
            steps {
                echo 'Launching Snake Game container...'
                sh 'docker run --rm snake-game-java'
            }
        }
    }
}
